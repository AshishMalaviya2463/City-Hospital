const initState = {
    isLoading: false,
    user: null,
    error: ""
}
export const authReducer = (state = initState, action) => {
    switch (action.type) {
        default:
            return state
    }
}